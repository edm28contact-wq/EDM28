{
  "name": "edm-auto-mecano-du-dimanche",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "build": "npm run check:syntax",
    "check:syntax": "find . -type f -name '*.js' -not -path './node_modules/*' -print0 | xargs -0 -n1 node --check",
    "dev": "vercel dev"
  },
  "engines": {
    "node": "24.x"
  }
}
