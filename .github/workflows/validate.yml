name: Validate

on:
  pull_request:
  push:
    branches:
      - pwa-edm28
      - feature/edm28-final-system
  workflow_dispatch:

permissions:
  contents: read

jobs:
  syntax:
    name: Validation pass ${{ matrix.pass }}
    runs-on: ubuntu-latest
    timeout-minutes: 8
    strategy:
      fail-fast: false
      matrix:
        pass: [1, 2, 3]

    steps